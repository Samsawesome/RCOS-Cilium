version: '3'

services:
  app1:
    build:
      context: ./app1
    ports:
      - "5000:5000"
    networks:
      - chat-network

  app2:
    build:
      context: ./app2
    ports:
      - "5001:5001"
    networks:
      - chat-network

networks:
  chat-network:
    driver: bridge
